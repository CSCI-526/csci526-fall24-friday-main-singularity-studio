using UnityEngine;

public class HeartTracker : MonoBehaviour
{
    [Tooltip("Unique numeric ID assigned manually to each heart in the Inspector")]
    public int heartID;           // Numeric ID for each heart, assigned manually in the Inspector

    [Tooltip("Name or label assigned manually to each heart in the Inspector")]
    public string heartName;       // Name or label for each heart, assigned manually in the Inspector
}


